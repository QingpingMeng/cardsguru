# CardsGuru does not require build-time secrets.
# Your GitHub Personal Access Token is entered in-app and stored locally on your device only.
# It is never read from environment variables and never committed.

# Optional: base path for static hosting under a subdirectory (e.g. GitHub Pages project site).
# CARDSGURU_BASE=/CardsGuru/
